[project]
name = "driver-behaviour-classifier"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "matplotlib",
    "pandas",
    "torch==2.3.1+cu121",
    "torchvision==0.18.1+cu121",
]
